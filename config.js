// ============================================================
// Al-Arabiyah Certificate Verification — Supabase connection
// ============================================================
// This file is already filled in and ready to upload as-is.
//
// SAFE TO PUBLISH: the publishable key below is meant to be used in the
// browser and is safe to have in a public GitHub repo, because Row Level
// Security (RLS) is enabled on the certificates table — it only ever
// exposes rows where status = 'valid'.
//
// NEVER put a Supabase "secret" / "service_role" key here — that key
// bypasses RLS and must never be exposed in a browser or public repo.
// ============================================================

window.ALC_SUPABASE_URL = "https://nptuftkvsfrbhekmwcob.supabase.co";
window.ALC_SUPABASE_KEY = "sb_publishable_bJ_nHCskPuW4c_32wkOD7w_FEYsQVPS";
